/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow all host headers for the proxy live preview
  experimental: {
    // If needed
  },
};

module.exports = nextConfig;
